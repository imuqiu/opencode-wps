# Word 校对通顺度/简洁度 — 技术设计文档

> **关联 Issue**: [#20](https://cnb.cool/lnxsun/opencode-wps/-/issues/20)（架构设计）、[#19](https://cnb.cool/lnxsun/opencode-wps/-/issues/19)（五维评分实现）
> **关联 PR**: [#40](https://cnb.cool/lnxsun/opencode-wps/-/pulls/40)（B 方案 — 2 工具 MCP 累加器）
> **作者**: lnxsun/opencode-wps(架构师)（设计）、lnxsun/opencode-wps(全栈开发工程师)（实现）
> **状态**: 设计完成，实现阶段
> **依赖**: 无（#22/#24 的前置判据基础）

---

## 目录

1. [架构概览](#1-架构概览)
2. [五维评分卡：通顺度](#2-五维评分卡通顺度)
3. [最小化测试：简洁度](#3-最小化测试简洁度)
4. [数据模型变更](#4-数据模型变更)
5. [Layer 1 → Layer 2 数据流](#5-layer-1--layer-2-数据流)
6. [报告输出格式](#6-报告输出格式)
7. [治理兼容性分析](#7-治理兼容性分析)
8. [风险评估](#8-风险评估)
9. [实现细节](#9-实现细节)
10. [数据流（MCP 工具层）](#10-数据流mcp-工具层)
11. [session_id 传递策略](#11-session_id-传递策略)
12. [架构评审决策记录](#12-架构评审决策记录)

---

## 1. 架构概览

### 1.1 五维定义

| 维度 | 英文 | 定义 | 典型问题类型 |
|------|------|------|-------------|
| 流畅度 | fluency | 成分完整、语句通顺 | 的得混淆、少字、口语化、错别字 |
| 简洁度 | conciseness | 无冗余、不啰嗦 | 重复字符、句式冗余、多字 |
| 准确性 | accuracy | 事实/术语/数据准确 | 法律术语错误、工程术语错误 |
| 一致性 | consistency | 格式规范、用语一致 | 中英混排、用词统一、异常空格 |
| 完整度 | completeness | 无占位文本、内容完整 | 占位文本（placeholder） |

### 1.2 现有架构（不改动）

```
                 getDocumentTextByRange (本批精确文本)
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
       ┌─────────────┐                ┌──────────────┐
       │  Layer 1     │                │  Layer 2      │
       │  proofread   │                │  AI 智能校对   │
       │  Basic       │                │  LLM 语义分析  │
       │  (正则引擎)   │                │  (五维评分卡)  │
       └──────┬───────┘                └──────┬────────┘
              │                               │
              └───────────┬───────────────────┘
                          ▼
                 confirmBatchAiProofread
                          ▼
                    结果合并去重
                          ▼
                  replaceInParagraph
```

### 1.3 本次变更范围

```
现有 Layer 1（不改架构，加规则）:
  proofread.ts rules[]  ∓ 新增 7 条规则（2 fluency + 5 conciseness）
  Rule 类型               ∓ 增加 metric 可选字段
  ProofreadIssue          ∓ 增加 metric 字段

现有 Layer 2（改提示词，加判据）:
  SKILL.md AI 提示词      ∓ 注入五维评分卡 + 最小化测试
  AI 输出 JSON 格式        ∓ 增加 metric 字段（枚举）

现有 governance.js（不改代码）:
  P15/P16 规则             ✓ 已支持 _force_ai_fix，无需改动
  P12-P14 规则             ✓ 无影响
```

### 1.4 新增概念模型

```
┌──────────────────────────────────────────────────┐
│                  通顺度评分卡                       │
│  ┌──────┬──────┬──────┬──────┬──────┐            │
│  │ ①成分 │ ②搭配 │ ③语序 │ ④句式 │ ⑤衔接 │ = /10    │
│  │ 完整  │ 得当  │ 自然  │ 干净  │ 连贯  │          │
│  ├──────┼──────┼──────┼──────┼──────┤            │
│  │ 0/1/2│ 0/1/2│ 0/1/2│ 0/1/2│ 0/1/2│          │
│  └──────┴──────┴──────┴──────┴──────┘            │
│                                                    │
│  ≥8 → 通顺（不报）                                  │
│  6-8 → 基本通顺可优化（仅报告带分数，不自动修复）      │
│  <6  → 不通顺（触发修复）                            │
│  单维 0 分 → 硬伤，无论总分均触发修复                │
└──────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────┐
│                  简洁度最小化测试                    │
│                                                    │
│  对每个候选冗余片段：                                │
│    删除 → LLM 自检 "语义是否完全不变"？              │
│    ├─ 语义不变 → 冗余 ✅（可删）                     │
│    └─ 语义变化 → 保留 ❌（不删）                     │
│                                                    │
│  简洁度 = 1 - (可删除字数 / 总字数)                   │
│                                                    │
│  冗余占比 ≥25% → 啰嗦（触发修复）                    │
│  冗余占比 10-25% → 可精简（仅报告不修）               │
│  冗余占比 <10%  → 简洁（不报）                       │
│                                                    │
│  ⚠️ 铁律：通顺 > 简洁                                │
│  任何简洁修复不得导致通顺度下降（不触发新通顺 issue）   │
└──────────────────────────────────────────────────┘
```

---

## 2. 五维评分卡：通顺度

### 2.1 评分总则

| 分值 | 含义 | 判定标准 |
|------|------|---------|
| **2 分** | 规范/自然 | 该维度无任何可感知问题，符合标准书面语习惯 |
| **1 分** | 可接受但可优化 | 有轻微不自然但不影响理解，或存在低频搭配但语感可接受 |
| **0 分** | 存在硬伤 | 明显语法错误、成分残缺、搭配不当，影响阅读理解 |

**关键判据**：**0 分是硬伤，必须修复；1 分是提升空间，视总分决定是否报。**

### 2.2 维度①：成分完整

> 判断主语、谓语、宾语是否完整，是否存在残缺或叠床架屋。

| 分值 | 判定 | 例句 | 说明 |
|------|------|------|------|
| **2** | 成分完整 | `公司决定扩大生产规模。` | 主谓宾齐全，修饰语位置正确 |
| **1** | 略松散但不缺 | `对于公司的这个决定，我们表示支持。` | 介词短语前置，主语后置，稍显松散但成分完整 |
| **0** | 成分残缺 | `通过这次会议，使得大家明确了方向。` | "通过…使得"双重介词导致主语缺失 |
| **0** | 成分残缺 | `由于成本上升，导致了利润下降。` | "由于…导致"使句子缺少独立主语 |
| **0** | 双主语 | `这个问题，我们需要认真对待它。` | "这个问题"和"它"构成双主语 |
| **0** | 缺宾语 | `公司制定了并实施了。` | "制定了"缺少宾语 |

**正则可检测模式（Layer 1）**：
- `通过(.*?)(使|让|令)` — 缺主语型句式杂糅 → `metric: fluency`
- ~~`由于(.*?)(导致|造成|使得)`~~ — 保留给 LLM（"由于成本上升导致利润下降"有时可接受）

**LLM 检测策略（Layer 2）**：
- 扫描句首介词链（"在…下/通过…/对于…/由于…"），检查后续是否有独立主语
- 检查动词是否有必要宾语
- 识别"被…所…"等双标记结构

### 2.3 维度②：搭配得当

> 判断动宾搭配、定中搭配、主谓搭配是否符合汉语习惯。

| 分值 | 判定 | 例句 | 说明 |
|------|------|------|------|
| **2** | 搭配规范 | `企业应履行社会责任。` | 动宾搭配符合习惯 |
| **1** | 搭配生僻但不误 | `强化了对风险的管理力度。` | "强化…力度"可接受但不精简 |
| **0** | 动宾搭配不当 | `我们应该履行这些作用。` | "作用"不可"履行"，应为"发挥…作用" |
| **0** | 定中搭配不当 | `这是一个严重的贡献。` | "严重"不可修饰"贡献" |
| **0** | 主谓搭配不当 | `产品的质量发生了飞跃。` | "质量"不能"发生飞跃"，应为"实现了飞跃" |
| **0** | 介词搭配错误 | `根据于调查结果，…` | "根据于"不存在，应为"根据" |

**正则可检测模式（Layer 1）**：
- 暂无高确定性规则（搭配判断高度依赖语境，正则误报率高）

**LLM 检测策略（Layer 2）**：
- 基于共现统计：检查高频动宾搭配、定中搭配
- 特别关注："履行+作用""达成+目的""实现+结果""取得+成绩"等
- 区分"正式搭配"与"口语搭配"（如"搞好关系"在正式公文中标记为1分）

### 2.4 维度③：语序自然

> 判断修饰语位置、否定词位置、状语位置是否符合汉语语序习惯。

| 分值 | 判定 | 例句 | 说明 |
|------|------|------|------|
| **2** | 语序自然 | `我们已按合同要求完成了所有工作。` | 状语位置正确 |
| **1** | 稍显欧化但可接受 | `我们完成了所有工作，按照合同的要求。` | 状语后置，口语中常见 |
| **0** | 语序错乱 | `我把作业没有做完。` | 否定词位置错误，应为"没有把作业做完" |
| **0** | 状语错位 | `他工作地在上海。` | 应为"他在上海工作" |
| **0** | 多重定语混乱 | `这是一家上海的致力于环保的科技创新公司。` | 多重定语堆砌，语序混乱 |
| **0** | 补语错位 | `他比我跑得快三分钟。` | 应为"他比我快三分钟"或"他跑得比我快三分钟" |

**正则可检测模式（Layer 1）**：
- 暂无高确定性规则（语序判断需要句法分析）

**LLM 检测策略（Layer 2）**：
- 状语位置：时间状语→地点状语→方式状语 的标准语序校验
- 否定词位置：`把字句`（否定词在"把"之前）、`被字句`（否定词在"被"之前）
- 多重定语排序：领属→时间地点→数量→动词性→形容词性→名词性

### 2.5 维度④：句式干净

> 判断是否存在句式杂糅、欧化长句、滥用被动、冗余框架。

| 分值 | 判定 | 例句 | 说明 |
|------|------|------|------|
| **2** | 句式干净 | `调查显示，用户满意度达到95%。` | 单一句式，表达直接 |
| **1** | 轻微冗余 | `需要指出的是，数据还存在一些误差。` | "需要指出的是"是框架废话但可容忍 |
| **0** | 句式杂糅 | `根据调查显示，用户满意度很高。` | "根据调查"+"调查显示"杂糅，选其一 |
| **0** | 句式杂糅 | `原因是由于管理不善造成的。` | "原因是"+"由于"+"造成的"三重杂糅 |
| **0** | 滥用长被动 | `该方案被经过多轮讨论后被认为是可以接受的。` | 多重被动嵌套 |
| **0** | 欧化长句 | `这是一个当我们在考虑方案的时候必须要面对的问题。` | 多层从句堆砌 |

**正则可检测模式（Layer 1）**：

| # | 正则 | 类型 | metric | 示例 |
|---|------|------|--------|------|
| 1 | `通过(.*?)(使\|让\|令)` | `句式杂糅` | `fluency` | "通过这次会议，**使**大家明确了方向" |
| 2 | `根据(.*?)显示` | `句式杂糅` | `fluency` | "**根据**调查**显示**" |

**⚠️ 正则捕获范围要求**：`original` 必须完整捕获整个冗余短语。例如规则1的 `original` 宜为 `"通过这次会议，使"` 而不是只捕获 `"使"` — 确保 P16 子串校验兼容。

**LLM 检测策略（Layer 2）**：
- 检测 `根据(.*?)认为/指出/显示`、`原因是(.*?)由于/因为/造成的` 等杂糅模式
- 识别"对于…而言""从…角度来看""在…背景下"等欧化框架
- 检测"被…所…""被…给…"等双被动标记
- "众所周知""需要注意的是""可以说""毫无疑问" → 标记为 1 分（冗余但不杂糅）

### 2.6 维度⑤：衔接连贯

> 判断关联词配对、指代清晰度、句间逻辑连贯性。

| 分值 | 判定 | 例句 | 说明 |
|------|------|------|------|
| **2** | 衔接连贯 | `由于成本上升，公司决定提价。然而，市场对此反应消极。` | 关联词匹配，指代清晰 |
| **1** | 衔接略弱 | `成本上升了。公司决定提价。反应消极。` | 无关联词，逻辑靠读者推断 |
| **0** | 关联词失配 | `虽然成本上升，但是公司决定提价。` | "虽然…但是"表示转折，但两句之间是因果关系，应为"因为…所以" |
| **0** | 指代不明 | `小明和小红去公园，他带了伞。` | "他"指代不明 |
| **0** | 关联词残缺 | `一方面成本在上升，公司需要提价。` | "一方面"缺少配对"另一方面" |
| **0** | 句间逻辑断裂 | `项目取得了成功。因此我们决定放弃。` | "成功"与"放弃"逻辑矛盾 |

**正则可检测模式（Layer 1）**：
- 暂无高确定性规则（衔接判断跨句，正则无法处理）

**LLM 检测策略（Layer 2）**：
- 关联词配对准：虽然→但是、因为→所以、不但→而且、一方面→另一方面
- 指代消解：检查代词（他/她/它/其/该）的先行词是否唯一且明确
- 逻辑一致性：检查关联词表达的转折/因果/递进是否与句子实际语义一致

### 2.7 评分示例

**示例 1：高分行（8-10 分）**

> `公司经过审慎研究，决定自下月起调整产品价格。该决定已获董事会批准，将在内部公告后正式执行。`

| 维度 | 评分 | 理由 |
|------|------|------|
| ① 成分完整 | 2 | 主谓宾齐全，无残缺 |
| ② 搭配得当 | 2 | "审慎研究""调整价格""获董事会批准"搭配规范 |
| ③ 语序自然 | 2 | 状语"经审慎研究"前置符合汉语习惯 |
| ④ 句式干净 | 2 | 无杂糅、无欧化 |
| ⑤ 衔接连贯 | 2 | "该决定"指代清晰，"将在"衔接自然 |
| **总分** | **10/10** | ✅ 通顺，不报 |

**示例 2：中间分（6-8 分）**

> `需要指出的是，根据我们对市场的调查显示，由于成本上升的原因，使得利润出现了下降的趋势。`

| 维度 | 评分 | 理由 |
|------|------|------|
| ① 成分完整 | 1 | "使得"前缺主语，但语义可补全 |
| ② 搭配得当 | 2 | 动宾搭配无问题 |
| ③ 语序自然 | 1 | 句首介词链过长 |
| ④ 句式干净 | 0 | **"根据…显示"杂糅 + "由于…的原因"冗余** |
| ⑤ 衔接连贯 | 2 | 句子间无断裂 |
| **总分** | **6/10** | ⚠️ 单维 0 分（④），触发修复 |

**示例 3：低分（<6 分）**

> `我把作业没有做完，因为昨天晚上的时候，我的电脑被死机了，导致于我无法继续做。`

| 维度 | 评分 | 理由 |
|------|------|------|
| ① 成分完整 | 1 | 成分完整但松散 |
| ② 搭配得当 | 0 | **"被死机"搭配不当**（死机是不及物动词） |
| ③ 语序自然 | 0 | **"我把作业没有做完"否定词错位** |
| ④ 句式干净 | 0 | **"的时候"冗余 + "导致于我"杂糅** |
| ⑤ 衔接连贯 | 1 | "因为"引导的原因从句后置，口语化 |
| **总分** | **2/10** | ❌ 不通顺，3 个单维 0 分，触发全面修复 |

---

## 3. 最小化测试：简洁度

### 3.1 算法定义

```
简洁度 = 1 - (可删除字数 / 句子总字数)

可删除字数 = Σ(语义不变的可删除片段字数)

判定器：对候选冗余片段 {删除后→原句→LLM 自检"语义是否完全不变？"}
```

### 3.2 冗余成分分类（4 类）

#### 类型 1：同义反复（确定性高，部分可正则）

| 冗余词组 | 建议 | metric |
|---------|------|--------|
| 大约…左右 | 删"左右"或"大约" | conciseness |
| 目的是为了 | 删"目的"或"为了" → "是为了" | conciseness |
| 首次开创 | 删"首次" → "开创" | conciseness |
| 亲眼目睹 | 删"亲眼" → "目睹" | conciseness |
| 更加进一步 | 删"更加" → "进一步" | conciseness |
| 属于…之一 | 删"之一" → "属于" | conciseness |
| 涉及到 | 删"到" → "涉及" | conciseness |

> **注意**：现有 Layer 1 规则已覆盖部分（如"涉及到""原因是由于"），本次新增的 5 条简洁规则不与之重复。

#### 类型 2：空洞填充词（确定性高，正则可覆盖）

| 冗余词 | 示例 | 修复 | metric |
|--------|------|------|--------|
| 进行 | `进行研究` → `研究` | 删"进行" | conciseness |
| 作出 | `作出决定` → `决定` | 删"作出" | conciseness |
| 予以 | `予以解决` → `解决` | 删"予以" | conciseness |
| 加以 | `加以完善` → `完善` | 删"加以" | conciseness |

**Layer 1 新增 4 条正则规则（#21 实施）**：

| # | 正则 | original 示例 | metric | type |
|---|------|-------------|--------|------|
| 3 | `进行(了)?(研究\|分析\|讨论\|处理\|调查)` | "进行研究" | conciseness | 冗余词 |
| 4 | `作出(了)?(决定\|部署\|安排)` | "作出部署" | conciseness | 冗余词 |
| 5 | `予以(了)?(解决\|处理\|落实)` | "予以落实" | conciseness | 冗余词 |
| 6 | `加以(了)?(解决\|完善\|规范)` | "加以规范" | conciseness | 冗余词 |

> **⚠️ 语境敏感度**：上述 4 条规则在以下场景中可能误报——法律文书（"作出如下决定"）、学术论文（"对数据进行了统计分析"）、正式公文（"现予以批复"）。因此这些规则的 `getSuggestion` 应生成建议性措辞（如"建议替换为'研究'，请复核语境"），而非断言式"自动删除"。Layer 1 保留正则检测的高召回率，Layer 2（P16 confirm 阶段）可利用完整上下文判定是否实际执行修复。

#### 类型 3：框架废话（确定性中，LLM 判定）

| 框架废话 | 说明 | metric |
|---------|------|--------|
| 我们需要注意的是 | 可删，直接说内容 | conciseness |
| 众所周知 | 可删，或保留（视语境） | conciseness |
| 可以说 | 可删 | conciseness |
| 需要指出的是 | 可删 | conciseness |
| 毫无疑问 | 可删 | conciseness |
| 针对…这一问题 | 可压缩为直接陈述 | conciseness |

**Layer 1 新增 1 条正则（#21 实施）**：

| # | 正则 | 示例 | metric | type |
|---|------|------|--------|------|
| 7 | `针对(.*?)这一问题` | "针对效率低下这一问题" | conciseness | 冗余词 |

#### 类型 4：可压缩从句（确定性低，LLM 判定）

| 冗余从句 | 修复 | metric |
|---------|------|--------|
| 在这个时代背景下 | 删或降级为"当前" | conciseness |
| 从某种意义上来说 | 删 | conciseness |
| 在某种程度上 | 删 | conciseness |
| 就目前而言 | 删或改为"目前" | conciseness |

> **归属**：类型 3 的第 7 条正则 + 类型 4 的全部留给 LLM 语境判断。

### 3.3 判定与修复规则

| 冗余占比 | 判定 | 动作 |
|----------|------|------|
| ≥ 25% | 啰嗦 | **触发修复**（出 issue + 建议） |
| 10% – 25% | 可精简 | **仅报告不修**（入"优化建议"小节） |
| < 10% | 简洁 | 不报 |

**执行期约束（铁律，须写进 SKILL.md）**：
1. **通顺 > 简洁**：任何简洁修复必须：① 通过"删除后语义不变"自检；② 修复后不得触发新的通顺 issue
2. 若简洁修复可能导致通顺度下降，即使冗余占比 ≥25%，也**只报告不修**，除非 LLM 能给出同时保持通顺的替代表述
3. 修复建议至少提供 2 个选项（保守/激进），用户可择其一
4. **简洁修复尝试最多 1 轮**（1 次修复 + 1 次通顺自检），若不通过则放弃修复，将该问题降级为「优化建议」并标注原因：`简洁修复可能影响通顺，建议人工处理`

---

## 4. 数据模型变更

### 4.1 `Rule` 类型扩展（proofread.ts）

```typescript
// 现有
type Rule = {
  pattern: RegExp;
  type: string;
  getSuggestion: (match: string) => string;
};

// 变更为
type Rule = {
  pattern: RegExp;
  type: string;
  getSuggestion: (match: string) => string;
  metric?: 'fluency' | 'conciseness';  // 新增：可选，向后兼容
};
```

### 4.2 `ProofreadIssue` 扩展

```typescript
// 现有
interface ProofreadIssue {
  offset: number;
  length: number;
  original: string;
  suggestion: string;
  type: string;
  context: string;
}

// 变更为
interface ProofreadIssue {
  offset: number;
  length: number;
  original: string;
  suggestion: string;
  type: string;
  context: string;
  metric?: 'fluency' | 'conciseness';  // 新增：Layer 1 由 Rule.metric 带出
}
```

### 4.3 Layer 2 AI 输出 JSON 扩展（SKILL.md）

```jsonc
// 现有
[{
  "paragraph_index": 1,
  "offset_in_paragraph": 23,
  "original": "...",
  "suggestion": "...",
  "reason": "..."
}]

// 变更为
[{
  "paragraph_index": 1,
  "offset_in_paragraph": 23,
  "original": "...",
  "suggestion": "...",
  "reason": "...",
  "metric": "fluency",           // 新增（必填）：枚举值 "fluency" | "conciseness"
  "score": {                     // 新增（当 metric=fluency 时必填）
    "total": 7,
    "dimensions": {
      "成分完整": 1,
      "搭配得当": 2,
      "语序自然": 2,
      "句式干净": 1,
      "衔接连贯": 1
    }
  },
  "redundancy_ratio": null,      // 新增（当 metric=conciseness 时必填）
  "minimality_test_passed": true // 新增（当 metric=conciseness 时必填）
}]
```

### 4.4 合并去重逻辑扩展

```typescript
// 现有
const deduped = allIssues.filter(issue => {
  const key = `${issue.offset}|${issue.original}`;
  if (seen.has(key)) return false;
  seen.add(key);
  return true;
});

// 不变 — metric 不参与去重，同一 offset+original 只留一条，
// 若 Layer 1 和 Layer 2 同时发现同一问题，保留 Layer 2 的（含 score 信息）。
// 去重时优先保留含 score 的条目。
```

---

## 5. Layer 1 → Layer 2 数据流

### 5.1 完整数据流

```
Step 1: Layer 1 执行（已扩展 metric 输出）
  proofreadBasic(text, startOffset) → {
    issues: [
      { offset, original, type: "句式杂糅", metric: "fluency", ... },
      { offset, original, type: "冗余词", metric: "conciseness", ... },
      ...
    ]
  }

Step 2: Layer 2 执行（新五维评分卡 + 最小化测试）
  AI 分析本批文本，逐句执行：
    2a. 五维评分 → 每句 { total, dimensions }
    2b. 若 total < 6 或 存在单维 0 → 出 issue（metric: fluency, score）
    2c. 若 total 6-8 且无 0 分 → 只报告（带分数），不出 issue
    2d. 最小化测试 → 计算冗余占比
    2e. 若冗余占比 ≥25% → 出 issue（metric: conciseness, redundancy_ratio）
    2f. 若冗余占比 10-25% → 只报告（带占比），不出 issue
  → issues: [{ ..., metric, score/redundancy_ratio }]

Step 3: confirmBatchAiProofread（不变）

Step 4: 合并去重
  - offset+original 去重
  - Layer 2 命中同一问题 → 保留 Layer 2 的（含 score 等元数据）
  - 统计按 metric 分组

Step 5: 修复
  - 常规修复：已匹配已知 issue → P16 自动放行
  - 语义性发现未匹配 Layer 1 issue → 走 _force_ai_fix 通道（附评分卡证据）
  - 6-8 分 + 10-25% 冗余 → 不进入修复，只进入报告
```

### 5.2 P16 兼容性分析

```
场景 A：正则规则命中（Layer 1 的 issue）
  → original 被 P16 收录
  → AI 修复时 findText 与 original 子串匹配
  → P16 自动放行 ✅

场景 B：LLM 发现但正则未命中（语义级问题）
  → 不在 P16 的 proofreadIssueOriginals 中
  → AI 修复需要 _force_ai_fix: true
  → 且 SKILL.md 明示：必须附评分卡证据（score < 6 或 单维 0）

场景 C：简洁修复
  → 若 Layer 1 未覆盖（类型 4 从句压缩等）
  → 同场景 B，走 _force_ai_fix
  → 额外要求：通顺 > 简洁自检通过
```

---

## 6. 报告输出格式

### 6.1 报告结构

```
{文档名}.校对报告.md

# 校对报告

- **文档**：XXX.docx
- **校对时间**：YYYY-MM-DD HH:mm
- **总字数**：XX,XXX
- **修订总数**：XX

## 发现的问题（已自动修复）

### 通顺问题
| # | 位置 | 原文 | 修改为 | 类型 | 通顺度 |
|---|------|------|--------|------|--------|
| 1 | 段落3 | 根据调查显示 | 调查显示 | 句式杂糅 | - |
| 2 | 段落5 | 通过会议使... | 会议使... | 句式杂糅 | 4/10 |

### 简洁问题
| # | 位置 | 原文 | 修改为 | 类型 | 冗余占比 |
|---|------|------|--------|------|---------|
| 1 | 段落8 | 进行研究 | 研究 | 冗余词 | - |
| 2 | 段落12 | 针对效率低...问题 | 效率低... | 冗余词 | 35% |

> **报告列策略**：默认隐藏「检测方式」列（不暴露内部实现细节）；在「详细模式」下展示「置信度」列——正则规则（MCP）标记为 ✅ 高置信，AI 检测标记为 ⚠️ 请人工复核。

## 优化建议（未自动修复）⚠️

> 以下问题已发现但未自动修复：通顺度 6-8 分或冗余占比 10-25%。
>
> **默认最多展示 Top 10**（通顺度按评分从低到高排序，冗余占比从高到低排序），超出部分折叠为「还有 N 条建议，详见完整报告」。通顺维度和简洁维度的 Top 10 分别独立计算（即最多共 20 条建议）。

| # | 位置 | 原文 | 建议 | 维度 | 评分/占比 |
|---|------|------|------|------|----------|
| 1 | 段落2 | ...需要指出的是... | 可考虑删除"需要指出的是" | 句式干净 | 6.5/10 |
| 2 | 段落7 | ...在某种程度上... | 可考虑删除 | 简洁度 | 18% |

## 统计摘要

| 类别 | 已修复 | 优化建议 | 合计 |
|------|--------|---------|------|
| 通顺 | X | Y | X+Y |
| 简洁 | A | B | A+B |
| **合计** | **X+A** | **Y+B** | **总计** |
```

### 6.2 报告数据源

```
已修复 section:
  - Layer 1 命中 + P16 放行 → metric 由 Rule.metric 带出
  - Layer 2 命中 + _force_ai_fix → metric 由 AI 输出的 metric 字段
  - 按 metric 分 fluency / conciseness 两个子表

优化建议 section:
  - 6-8 分（通顺可优化）→ 由 Layer 2 AI 输出（有 score 但未触发修复）
  - 10-25% 冗余（可精简）→ 由 Layer 2 AI 输出（有 redundancy_ratio 但未触发修复）
  - 仅 AI 层能产出此 section（Layer 1 正则只产 issue，不产"建议"）

统计摘要:
  - 按 metric 分组，从 issue 和 建议 中聚合
```

---

## 7. 治理兼容性分析

### 7.1 P15/P16 不变

| 规则 | 现有行为 | 本次影响 | 结论 |
|------|---------|---------|------|
| **P15** | `proofreadHadIssues=false` 时限制 AI 修复（≤1 次） | 正则新增 7 条规则后，更多冗余词被 Layer 1 捕获 → `proofreadHadIssues` 更可能为 true → P15 放行次数增加 | ✅ 正向，更安全 |
| **P16** | 替换必须与已知 issue 的 original 子串匹配 | 正则规则的 `original` 捕获完整短语 → 子串匹配成功率更高 | ✅ 正向，更安全 |
| `_force_ai_fix` | 已支持，手动绕过 P15/P16 | 用于 LLM 语义发现（正则未命中的），需 SKILL.md 明示使用条件 | ✅ 通道已存在 |

### 7.2 P12/P14 不变

分批校对循环对通顺/简洁判定无影响。AI 在新框架下仍需逐批完成 `proofreadBasic → confirmBatchAiProofread → replaceInParagraph`。

### 7.3 结论

**governance.js 代码无需改动。** 本次变更完全在 Layer 1（规则引擎）和 Layer 2（AI 提示词）层面实现，P15/P16 已提供足够的兼容通道。

---

## 8. 风险评估

| 风险项 | 影响 | 概率 | 缓解措施 |
|--------|------|------|----------|
| AI 评分不一致（同一句子两次评分差 ≥3） | 中 | 高 | 固定评分卡维度+判据；报告中标注置信度；用户可拒收单条修订 |
| 正则误报（新规则匹配合法文本） | 中 | 低 | 首批 7 条规则选最高确定性；已剔除"对…而言""由于…导致"等争议模式 |
| AI 过度修改通顺句子（把对的改错） | 高 | 中 | 6-8 分不入修复；单维 0 才触发；修订模式全链路可追溯 |
| 简洁修复改变原意 | 高 | 中 | 强制最小化测试（语义不变自检）；通顺>简洁执行期约束；`_force_ai_fix` 二次确认 |
| `metric` 字段未正确传递到报告 | 低 | 中 | 单测断言 Rule→Issue→合并→报告全链路 metric 传递 |
| 报告统计失真（AI 层 metric 枚举值非预期） | 低 | 低 | AI 输出格式显式约束 metric 枚举值（`fluency` / `conciseness`），禁止编造 |

---

## 9. 实现细节

以下内容由全栈开发工程师在 PR #40 中实现。更多实现细节见代码文件 `wps-office-mcp/src/tools/word/proofread-report.ts`。

### 9.1 维度合并说明

**`standardization`（规范性）维度在 Layer 1 实现中合并入 `consistency`（一致性）**。

原因：Layer 1 正则规则检测到的"规范性问题"（中英混排、数字空格、中文标点、用词统一、异常空格）本质上属于格式一致性问题——规范即一致性。合并后 `consistency` 维度涵盖了所有格式层面的问题。

AI Layer 2 中 `standardization` 仍可独立存在，用于检测更复杂的规范性语义问题（如措辞风格、行业规范等）。

**`completeness`（完整度）为 Layer 1 新增维度**。

原因：占位文本检测（如 "xxx有限公司"、"TODO"）是一个独立于其他四个维度的概念——它不涉及语言质量，而是内容完整性。占位文本属于 blocker 级问题（有则得 0 分）。

### 9.2 评分量表与归一化

**Layer 1 原始分**：1-5 量表（`METRIC_WEIGHT_FORMULA` 公式计算，completeness 可能为 0）

**Layer 2 AI 评分卡**：0/1/2 三级制

**归一化路径**：

```
Layer 1: rawScore (1-5) → normalizeToTwoPointScale → [0, 2]
Layer 2: 0/1/2 (直接)

综合分: min(L1_norm, L2) × 5 → X.X/10
```

归一化公式：`normalized = max(0, min(2, (rawScore - 1) / 2))`

| rawScore | normalized | display |
|----------|-----------|---------|
| 5.0     | 2.00      | 10.0/10 |
| 4.0     | 1.50      | 7.5/10  |
| 3.0     | 1.00      | 5.0/10  |
| 2.0     | 0.50      | 2.5/10  |
| 1.0     | 0.00      | 0.0/10  |
| 0.0     | 0.00      | 0.0/10  |

### 9.3 TYPE_METRIC_MAP

完整映射表见 `wps-office-mcp/src/tools/word/proofread-report.ts` 中的 `TYPE_METRIC_MAP`。

### 9.4 权重公式

```typescript
const METRIC_WEIGHT_FORMULA = {
  fluency:       (c) => Math.max(0, 5 - c * 0.2),
  conciseness:   (c) => Math.max(0, 5 - c * 0.3),
  accuracy:      (c) => Math.max(0, 5 - c * 1.0),
  consistency:   (c) => Math.max(0, 5 - c * 0.1),
  completeness:  (_c, hasPlaceholder) => hasPlaceholder ? 0 : 5,
};
```

| 维度 | 权重 | 下限 |
|------|------|------|
| fluency（流畅度） | 5 - count×0.2 | 0 |
| conciseness（简洁度） | 5 - count×0.3 | 0 |
| accuracy（准确性） | 5 - count×1.0 | 0 |
| consistency（一致性） | 5 - count×0.1 | 0 |
| completeness（完整度） | hasPlaceholder?0:5 | 0 |

---

## 10. 数据流（MCP 工具层）

本节描述 MCP 工具层的 session-based 数据流，由 PR #40 实现的两个报告工具驱动（统一经 `wps_office_execute` 网关调用，网关路由到对应 MCP handler）。

```
┌─────────────┐     ┌──────────────────┐     ┌─────────────────────────┐
│ SKILL.md    │────▶│ AI 生成 session_id │────▶│ MCP Server              │
│ 引导 AI     │     │ UUID v4           │     │ sessionIssues Map       │
│ 调用工具    │     └──────────────────┘     │ (与 governance.js 解耦)  │
└─────────────┘              │               └───────────┬─────────────┘
                              │                           │
                              ▼                           ▼
                     ┌────────────────┐         ┌──────────────────┐
                     │ 每批校对完成后  │────────▶│ proofread_       │
                     │ 调用 accumulate │         │ accumulate       │
                     └────────────────┘         └────────┬─────────┘
                                                         │
                                                         ▼
                                                ┌──────────────────┐
                                                │ 所有批次完成后    │
                                                │ generate_report  │
                                                └──────────────────┘
```

**工作流**：
1. AI 按 SKILL.md 指引生成 `session_id`（UUID v4）
2. 每批校对完成后，将 issues 通过 `wps_word_proofread_accumulate` 累积到 `sessionIssues` Map
3. 所有批次完成后，调用 `wps_word_generate_proofread_report` 生成五维评分报告
4. `sessionIssues` Map 与 governance.js 的 `sessions` Map 完全解耦（两个进程、两个内存空间）

---

## 11. session_id 传递策略

- `session_id` 由 AI 手动生成（UUID v4），在 `inputSchema` 中显式声明为必填参数
- SKILL.md 引导 AI 生成并传递 `session_id`
- `wps_word_proofread_accumulate` 和 `wps_word_generate_proofread_report` 在 MCP Server 注册为直接工具，同时加入 gateway `COM_ACTIONS` 索引兜底（`proofreadAccumulate` / `generateProofreadReport`），**统一经 `wps_office_execute` 网关调用**（网关路由到对应 handler）
- 双通道可用：直接 MCP 调用与网关调用均可达，但 SKILL.md 引导统一走网关（用户侧 MCP 工具仅能通过网关调用）
- MCP Server 侧 `sessionIssues` Map 与 governance.js 的 `sessions` Map 完全解耦（两个进程、两个内存空间）

---

## 12. 架构评审决策记录

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | `session_id` 传递方式 | SKILL.md 引导 AI 手动传 |
| 2 | `standardization` → `consistency` 合并 | Layer 1 合并入 consistency，AI Layer 2 仍独立 |
| 3 | `completeness` 新维度 | Layer 1 新增，占位文本检测 |
| 4 | `少字` 分类 | fluency（成分残缺），非 accuracy |
| 5 | 五维评分量表归一化 | `normalizeToTwoPointScale()` [1,5] → [0,2] |
| 6 | 权重公式下限 | fluency 下限 0（非 1）；completeness 下限 0（非 1） |
| 7 | governance.js 与 MCP Map | 完全独立，不共享 |
| 8 | `generateProofreadReport` / `proofreadAccumulate` 走不走网关 | **走网关**（`wps_office_execute` 路由到 handler；同时保留直接 MCP 注册作为兜底） |

---

## 附录 A：正则规则完整清单（#21 实施）

| # | 正则 | 类型 | metric | original 捕获 | 示例命中 | 建议 |
|---|------|------|--------|-------------|---------|------|
| 1 | `通过(.*?)(使\|让\|令)` | `句式杂糅` | `fluency` | "通过这次会议，使" | "**通过这次会议，使**大家明确了方向" | 删除冗余介词，保留核心语义 |
| 2 | `根据(.*?)显示` | `句式杂糅` | `fluency` | "根据调查显示" | "**根据调查显示**，满意度很高" | 选用其一："调查显示"或"根据调查" |
| 3 | `进行(了)?(研究\|分析\|讨论\|处理\|调查)` | `冗余词` | `conciseness` | "进行研究" | "我们需要**进行研究**" | ⚠️ 建议替换为直接动词（如"研究"），请复核语境 |
| 4 | `作出(了)?(决定\|部署\|安排)` | `冗余词` | `conciseness` | "作出部署" | "公司已**作出部署**" | ⚠️ 建议替换为直接动词（如"部署"），请复核语境 |
| 5 | `予以(了)?(解决\|处理\|落实)` | `冗余词` | `conciseness` | "予以落实" | "需尽快**予以落实**" | ⚠️ 建议替换为直接动词（如"落实"），请复核语境 |
| 6 | `加以(了)?(解决\|完善\|规范)` | `冗余词` | `conciseness` | "加以规范" | "应对流程**加以规范**" | ⚠️ 建议替换为直接动词（如"规范"），请复核语境 |
| 7 | `针对(.*?)这一问题` | `冗余词` | `conciseness` | "针对效率低下这一问题" | "**针对效率低下这一问题**" | 压缩为直接陈述 |

> **与现有规则无重叠**：现有 rules 已有"涉及到""目的是为了""原因是由于""必须要""全部都""现如今""最为"等规则，新增 7 条使用独立的 type 标记（`句式杂糅` / `冗余词`），确保不重复。

## 附录 B：LLM 提示词评分卡模板（#22 注入 SKILL.md）

```
## 通顺度五维评分卡（逐句评分）

对当前句子从以下 5 个维度各打 0/1/2 分：

| 维度 | 0 分（硬伤） | 1 分（可优化） | 2 分（规范） |
|------|-------------|---------------|-------------|
| 成分完整 | 缺主语/缺宾语/双主语 | 介词短语前置但成分全 | 主谓宾齐全 |
| 搭配得当 | 动宾不当/主谓不当 | 搭配生僻但可接受 | 搭配规范 |
| 语序自然 | 否定词错位/状语错位 | 稍欧化但可接受 | 语序自然 |
| 句式干净 | 句式杂糅/滥用被动 | 含框架废话 | 句式干净 |
| 衔接连贯 | 关联词失配/指代不明 | 无关联词但可推断 | 衔接自然 |

判定规则：
- 总分 ≥8 且无 0 分 → 通顺，不出 issue
- 总分 6-8 且无 0 分 → 只报告分数（优化建议），不出 issue
- 总分 <6 或 存在 0 分 → 出 issue，附 score 对象

对冗余成分：
- 逐片段执行最小化测试（删除 → 语义是否不变）
- 冗余占比 ≥ 25% → 出 issue（metric: conciseness）
- 冗余占比 10-25% → 只报告占比（优化建议），不出 issue

通顺 > 简洁铁律：任何简洁修复不得导致新通顺 issue
```

---

> **下一步**：#21（Layer 1 规则引擎）和 #22（Layer 2 AI 提示词）可依此设计文档并行启动。

---

## 附录 C：不在本次范围

以下内容明确排除在本次变更范围之外，以防止范围蔓延：

- ❌ 标点符号校对（已有独立规则）
- ❌ 排版格式校对（字体/字号/行距）
- ❌ 事实性错误检测（数据、日期、人名验证）
- ❌ 语气/风格一致性检测（如正式度统一）
- ❌ 用户自定义评分阈值（首期使用固定阈值：6 分 / 25%）
- ❌ 跨句连贯性深度分析（如篇章结构、段落逻辑）
- ❌ 多语言混合校对（仅限中文文本）
- ❌ 校对历史/版本对比功能
